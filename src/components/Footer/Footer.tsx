import React from 'react';
import { Wrapper } from './styles.Footer';

export const AppFooter: React.FC = () => {
    return <Wrapper>Ein Prototyp aus dem AI + Automation Lab des Bayerischen Rundfunks.</Wrapper>;
};
